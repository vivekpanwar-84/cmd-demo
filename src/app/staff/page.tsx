import { AdminStaff } from "@/components/dashboard/AdminStaff";

export default function StaffPage() {
    return <AdminStaff organizationId="1" />; // Default to "1" for now or handle differently? Original App.tsx passed "1".
}